server:
  port: 8785
  
spring:
  application:
    name: cloud-config-server
  profiles:
    active: native
  cloud:
    config:
      label: master
      server:
        native:
          searchLocations: classpath:config/
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/