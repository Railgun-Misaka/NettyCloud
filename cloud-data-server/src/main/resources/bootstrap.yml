spring:
  cloud:
    config:
      name: native
      profile: common
      label: master
      discovery:
        enabled:  true
        serviceId:  cloud-config-server
  client:
    serviceUrl:
      defaultZone:  http://localhost:8761/eureka/